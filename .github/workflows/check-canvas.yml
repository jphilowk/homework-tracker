name: Check Canvas connection

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: canvas-connection-check
  cancel-in-progress: false

jobs:
  check:
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Check out reviewed code
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          persist-credentials: false
      - name: Set up Node
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: '24'
      - name: Test connector and application without credentials
        run: npm test
      - name: Check Canvas without exposing student data
        env:
          CANVAS_TOKEN: ${{ secrets.CANVAS_TOKEN }}
        run: node scripts/check-canvas.mjs
